# Where this repository lives, and which law applies here

*Read this first. It is a pointer, not a rulebook - it holds no rule text of its own.*

- **Estate:** ECS, the operator's estate, `*.workshop.home`. This checkout lives on
  **barn.workshop.home** at `/home/billyb/workspaces/ztip`.
- **What applies here:** This is a **ZTI-side repository**, not a Billy Workspace project. The Billy / ZTI separation contract governs the boundary; the workspace contract does not rule here.
- **Where the rules actually are:** one published bundle, served by the gate on
  **farmhouse.workshop.home**. Read it with `ssh ecs-gate@farmhouse.workshop.home docs`, and only what changed with
  `ssh ecs-gate@farmhouse.workshop.home docs --changed`. On claude.workshop.home the same bundle is `mywork docs`.
  Check the revision every session with `ssh ecs-gate@farmhouse.workshop.home revision`. Nothing cached anywhere
  overrides the served bundle.
- **Where you are, in one command:** `ecs-here` prints the situation block - host, side of the
  estate boundary, this repository and its law, the revision, and whether your identity has
  acknowledged it.
- **Where the estate ends:** `web1.bitscon.net` and the public VPS are **outside** the estate.
  ECS law does not govern them and deploying there is operator-gated.
- **Billy, both ways:** outside `billy-runtime`, hands off Billy. Inside it, the Architect
  contract and the ledger govern the work, and the estate's house rules still govern how you
  speak to the operator and record the work on the board.
- **Project law:** whatever this repository's own `AGENTS.md` and its `docs/` say. It is
  narrower than the above and never overrides it.

This file is generated by `ecs-here --pointer` from one template in the estate repository
(`bitscon/ecs`, `governance/tools/ecs-here`). Do not hand-edit it: the check rejects a copy that
has drifted from the template, and a rule written in here instead of in the bundle is exactly the
drift this file exists to prevent.

<!-- ecs-here v1 variant=zti repo=ztip fingerprint=a1140eb39106 -->
